name: task-scheduler

services:
  postgres:
    image: postgres:15-alpine
    container_name: task-scheduler-postgres
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: scheduler_admin
      POSTGRES_PASSWORD: scheduler_password
      POSTGRES_DB: task_scheduler
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U scheduler_admin -d task_scheduler"]
      interval: 5s
      timeout: 5s
      retries: 5
  redis:
    image: redis:7-alpine
    container_name: task-scheduler-redis
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  api:
    build: .
    container_name: task-scheduler-api
    ports:
      - "8001:8000"
    environment:
      PYTHONUNBUFFERED: "1"
      DATABASE_URL: postgresql://scheduler_admin:scheduler_password@postgres:5432/task_scheduler
      REDIS_URL: redis://redis:6379/0
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  worker-alpha:
    build: .
    container_name: task-worker-alpha
    command: python -m scheduler.worker --api-url http://api:8000 --redis-url redis://redis:6379/0
    environment:
      PYTHONUNBUFFERED: "1"
      WORKER_ID: worker-alpha
      DATABASE_URL: postgresql://scheduler_admin:scheduler_password@postgres:5432/task_scheduler
      SCHEDULER_API_URL: http://api:8000
      REDIS_URL: redis://redis:6379/0
    depends_on:
      redis:
        condition: service_healthy
      api:
        condition: service_started

  worker-beta:
    build: .
    container_name: task-worker-beta
    command: python -m scheduler.worker --api-url http://api:8000 --redis-url redis://redis:6379/0
    environment:
      PYTHONUNBUFFERED: "1"
      WORKER_ID: worker-beta
      DATABASE_URL: postgresql://scheduler_admin:scheduler_password@postgres:5432/task_scheduler
      SCHEDULER_API_URL: http://api:8000
      REDIS_URL: redis://redis:6379/0
    depends_on:
      redis:
        condition: service_healthy
      api:
        condition: service_started

  worker-gamma:
    build: .
    container_name: task-worker-gamma
    command: python -m scheduler.worker --api-url http://api:8000 --redis-url redis://redis:6379/0
    environment:
      PYTHONUNBUFFERED: "1"
      WORKER_ID: worker-gamma
      DATABASE_URL: postgresql://scheduler_admin:scheduler_password@postgres:5432/task_scheduler
      SCHEDULER_API_URL: http://api:8000
      REDIS_URL: redis://redis:6379/0
    depends_on:
      redis:
        condition: service_healthy
      api:
        condition: service_started

  sweeper:
    build: .
    container_name: task-scheduler-sweeper
    command: python -m scheduler.sweeper
    environment:
      PYTHONUNBUFFERED: "1"
      DATABASE_URL: postgresql://scheduler_admin:scheduler_password@postgres:5432/task_scheduler
      REDIS_URL: redis://redis:6379/0
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy

  test:
    build:
      context: .
      dockerfile: Dockerfile.test
    container_name: task-scheduler-test
    environment:
      PYTHONUNBUFFERED: "1"
      DATABASE_URL: sqlite:///./unit_test.db
    command: pytest -q tests/test_scheduler.py

  prometheus:
    image: prom/prometheus:v2.54.1
    container_name: task-scheduler-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    depends_on:
      api:
        condition: service_started

  grafana:
    image: grafana/grafana:11.1.0
    container_name: task-scheduler-grafana
    ports:
      - "3000:3000"
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin
    volumes:
      - ./monitoring/grafana/provisioning/datasources:/etc/grafana/provisioning/datasources:ro
      - ./monitoring/grafana/provisioning/dashboards:/etc/grafana/provisioning/dashboards:ro
      - ./monitoring/grafana/dashboards:/var/lib/grafana/dashboards:ro
    depends_on:
      prometheus:
        condition: service_started

volumes:
  postgres_data:
  redis_data:
